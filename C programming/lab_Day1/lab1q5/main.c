#include <stdio.h>
#include <stdlib.h>

int main()
{
        int x,y;
        printf("please input two integer x,y\n ");
        scanf("%d",&x);
        scanf("%d",&y);
        printf("add is=%d\n",x+y);
        printf("sub is=%d\n",x-y);
        printf("multiply is=%d\n",x*y);
        printf("divid is=%d\n",x/y);
    return 0;
}
